# <NN> — `<test-name>` 검증

## 1. 이 테스트가 검증하는 것
`<이 테스트의 핵심 검증 포인트>`

## 2. 구현 필요 함수와 규칙
### `<함수명>`
- 위치: `<파일 경로>`
- 규칙 1: `<필수 규칙>`
- 규칙 2: `<필수 규칙>`

### `<함수명>`
- 위치: `<파일 경로>`
- 규칙 1: `<필수 규칙>`
- 규칙 2: `<필수 규칙>`

## 3. 실패 시 바로 확인할 것
- `<체크 포인트 1>`
- `<체크 포인트 2>`
- `<체크 포인트 3>`

## 4. 재검증
- `<해당 테스트>` 단일 실행 통과 확인
- 필요 시 `<연관 테스트>` 재실행
